# .github/workflows/ci.yml
# This workflow defines our Continuous Integration (CI) pipeline.
# It will run automatically on every push and pull request to the main branch.

name: ScholarAgent CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11"] # Test against multiple Python versions

    steps:
      # --- Step 1: Check out the repository code ---
      - name: Check out repository
        uses: actions/checkout@v4

      # --- Step 2: Set up the specified Python version ---
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      # --- Step 3: Install dependencies ---
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # --- Step 4: Lint with Ruff ---
      # This step checks for code style and formatting issues.
      - name: Lint with Ruff
        run: |
          ruff check src/

      # --- Step 5: Run tests with pytest ---
      # This is the critical step that runs our unit tests.
      - name: Run tests
        run: |
          python -m pytest
